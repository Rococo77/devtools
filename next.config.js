/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Ajoutez ici d'autres options de configuration que vous souhaitez inclure
};

module.exports = nextConfig;